namespace MyWeb.Utility
{
    public static class HtmlHelperExtension
    {
        //public static MvcHtmlString PersianDateTimePickerFor<TModel, TValue>(this HtmlHelper<TModel> html, Expression<Func<TModel, TValue>> expression)
        //{

        //    var tt = typeof(TValue);
        //    if (typeof(TValue)!=null)
        //    {
        //        IFormatProvider formatProvider = new System.Globalization.CultureInfo("fa-IR");
        //        html.TextBoxFor(expression, new { @class = "form-control datepicker", autocomplete = "off", Value = typeof(TValue)?.ToString("yyyy/MM/dd HH:mm", formatProvider) });
        //    }
        //    else
        //    {
        //        IFormatProvider formatProvider = new System.Globalization.CultureInfo("fa-IR");
        //        html.TextBoxFor(expression, new { @class = "form-control datepicker", Value = DateTime.Now.ToString("yyyy/MM/dd HH:mm", formatProvider) })
        //    }

        //    return html.TextBoxFor(expression, new { @class = "form-control", autocomplete = "off" });
        //}
    }
}
